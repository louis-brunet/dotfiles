#!/usr/bin/env bash
# Node module uninstall script

set -e

echo "Uninstalling node module..."

# Remove nvm (optional - might break other things using nvm)
# For now just clean up

echo "✅ Node module uninstalled (tools retained - may need manual cleanup)"